version: '3.8'

services:
  ryde-web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:3000"
    environment:
      - NODE_ENV=production
      - PORT=8080
      - NEXT_PUBLIC_API_BASE_URL=https://api.rydeflexi.com
    restart: always
